# Obsidian live capability

The vault is a filesystem of Markdown notes. The core integration does not require an Obsidian plugin.

Use `scripts/vault.py` to:
- create a session note,
- append a live transcript,
- open the note in Obsidian.

Agents should write only to the active session/research notes they were asked to use.
Permanent knowledge notes require human approval.
